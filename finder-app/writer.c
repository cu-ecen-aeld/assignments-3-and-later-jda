/* given 2 args, first is dir, second is search string,
   count files in dir recursively and count number of times
   search string appears in those files */

int main(int argc, char **argv) {
	return 0;
}